{
  "name": "openkanji-api",
  "private": true,
  "type": "module",
  "description": "OpenKanji progress sync -- Cloudflare Worker",
  "scripts": {
    "test": "node --experimental-sqlite --no-warnings test/worker.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  }
}
